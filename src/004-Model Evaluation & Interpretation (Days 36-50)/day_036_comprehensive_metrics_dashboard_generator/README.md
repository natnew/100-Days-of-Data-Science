# Comprehensive Metrics Dashboard Generator

AI selects relevant evaluation metrics based on problem type and stakeholder needs, explaining metric significance.

## 🚀 How to Run

1. **Install Dependencies**:
   Ensure you are in the project root directory (where `requirements.txt` is located):
   ```bash
   pip install -r requirements.txt
   ```

2. **Configure Environment**:
   Make sure you have a `.env` file in the project root directory with your API keys (see `.env.example`).

3. **Run the Project**:
   Navigate to this project folder and execute the script:
   ```bash
   python main.py
   ```
